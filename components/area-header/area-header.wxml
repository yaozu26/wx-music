<view class="header">
  <view class="title">{{title}}</view>
  <view class="more" wx:if="{{more}}" bindtap="onmoreClick">
    <view class="text">{{more}}</view>
    <van-icon name="arrow" />
  </view>
</view>